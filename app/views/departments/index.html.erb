<h1>Department List</h1>

<table class="table">
  <tr>

    <th colspan="3"></th>
  </tr>
 
  <% @departments.each do |department| %>
    <tr>
      <td><%= department.name %><%= link_to 'Edit', edit_department_path(department) %>
   <%= button_to 'Delete', department_path(department),method: :delete %></td>

  <% end %>
  <td><%= link_to 'New Department', new_department_path %></td>
<%= link_to 'Home', welcome_index_path %>
   </div>
      
</table>